from __future__ import annotations

"""
LEGADO INATIVO — V225

Este módulo foi preservado apenas para rastreabilidade histórica.

A rota oficial não deve importar este arquivo. Caso alguma saída antiga seja
necessária, migrar primeiro o contrato para `nucleo/saida_observavel.py` ou
`nucleo/saida_canonica.py`.
"""


def render_secao_canonicas(*args, **kwargs):
    """Função legada neutralizada."""
    raise RuntimeError(
        "render_secao_canonicas está neutralizada. "
        "Migre o contrato para nucleo/saida_observavel.py ou nucleo/saida_canonica.py antes de reativar."
    )
