# Tests

This folder is reserved for future automated tests.

The repository is currently in a methodological implementation phase, so the next recommended additions are:

- normalization tests,
- historical reconstruction tests,
- pricing engine tests,
- future diagnosis regression tests.
