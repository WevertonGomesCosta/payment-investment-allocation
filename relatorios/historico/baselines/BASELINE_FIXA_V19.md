# Baseline fixa V19

Esta versão corrige duas incoerências identificadas após a V18:

- remoção do fallback nominal específico de `CDB Turbinado` no matching canônico;
- enquadramento explícito do score v1 apenas como **triagem preliminar proxy** do motor.
